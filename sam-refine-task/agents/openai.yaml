interface:
  display_name: "Sam Refine Task"
  short_description: "Evidence-led strategy refinement without edits"
  default_prompt: "Use $sam-refine-task to challenge this strategy, close material loopholes, and return a validated confidence decision without implementing it."
