interface:
  display_name: "Sam Work"
  short_description: "Run the complete verified delivery workflow"
  default_prompt: "Use $sam-work to deliver this task through every required gate and prove completion. Never ask for permission or confirmation on any step; execute fully or report BLOCKED with exact failures."
