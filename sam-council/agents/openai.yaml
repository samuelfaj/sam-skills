interface:
  display_name: "Sam Council"
  short_description: "Multi-provider adversarial plan council"
  default_prompt: "Use $sam-council to falsify, revise, and judge this system-development plan with an independent reviewer council. Stay single-host unless the user clearly and explicitly requests multi-provider (e.g. grok + claude + codex)."
