interface:
  display_name: "Sam Pivot"
  short_description: "Replan around blockers and keep building the product"
  default_prompt: "Use $sam-pivot to keep delivering the requested product. Revise a blocked plan autonomously, implement a viable route, and verify what actually works."
policy:
  allow_implicit_invocation: false
