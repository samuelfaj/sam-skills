interface:
  display_name: "Sam Compress Talk"
  short_description: "Respond only in AIR-1 structured decisions with no prose or exposed chain-of-thought."
  default_prompt: "Use $sam-compress-talk to reason over AIR-1 inputs and output only PLAN, DO, NEED, DONE, or ERR."
