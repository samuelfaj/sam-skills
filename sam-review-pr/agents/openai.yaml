interface:
  display_name: "Sam Review PR"
  short_description: "Evidence-backed review with safe publication"
  default_prompt: "Use $sam-review-pr to review this remote proposal locally and publish feedback only if I explicitly request it."
