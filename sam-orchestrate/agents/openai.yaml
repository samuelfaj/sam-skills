interface:
  display_name: "Sam Orchestrate"
  short_description: "Coordinate useful delegation with verified integration"
  default_prompt: "Use $sam-orchestrate to delegate independent work when it saves time. Use the current host's default routing, then verify the integrated outcome."
