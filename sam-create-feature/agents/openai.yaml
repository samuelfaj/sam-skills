interface:
  display_name: "Sam Create Feature"
  short_description: "Evidence-backed feature delivery with frozen scope"
  default_prompt: "Use $sam-create-feature to implement this capability with frozen scope, calibrated tests, and validated proof."
