interface:
  display_name: "Sam Create Feature"
  short_description: "Feature workflow with requirements, refine loop, TDD, validation, and PR evidence."
  default_prompt: "Use $sam-create-feature to discover requirements, run the pre-test refine loop, implement the feature with TDD, validate it, and create a draft PR/MR with evidence."
