interface:
  display_name: "Sam Task"
  short_description: "Plan, refine, deliver, then close findings"
  default_prompt: "Use $sam-task to plan, refine, deliver with $sam-work, then loop $sam-review and $sam-council until no material problems remain. Never ask mid-run; execute fully or report BLOCKED with exact failures."
