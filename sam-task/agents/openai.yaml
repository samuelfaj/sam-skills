interface:
  display_name: "Sam Task"
  short_description: "Plan, deliver, close, and audit learning"
  default_prompt: "Use $sam-task to plan, refine, deliver with $sam-work, close findings through $sam-review and $sam-council, then audit evidence for proposal-only reusable learning. Never ask mid-run; execute fully or report BLOCKED with exact failures."
