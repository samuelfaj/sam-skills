interface:
  display_name: "Sam Task"
  short_description: "Complete a software task with proportional proof"
  default_prompt: "Use $sam-task to finish the requested task at its requested delivery point. Inspect first, choose the shortest sound route, verify the result, and seek new paths around real blockers."
