interface:
  display_name: "Sam Create Test Coverage"
  short_description: "Risk-based test coverage across unit, component, integration, API, and E2E layers."
  default_prompt: "Use $sam-create-test-coverage to map affected behavior, add the smallest meaningful tests for confidence, run relevant validation, and report exact blockers."
