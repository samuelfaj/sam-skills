interface:
  display_name: "Sam Create Test Coverage"
  short_description: "Create validated multi-layer regression coverage"
  default_prompt: "Use $sam-create-test-coverage to map changed behavior, add the smallest reliable tests, and validate every proof gap."
