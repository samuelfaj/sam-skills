interface:
  display_name: "Sam Review Code"
  short_description: "Rigorous local code review with strict structural quality checks returned in Codex."
  default_prompt: "Use $sam-review-code to review the local code changes end-to-end, include strict maintainability checks, and return the review in Codex only."
