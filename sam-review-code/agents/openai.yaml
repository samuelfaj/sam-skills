interface:
  display_name: "Sam Review Code"
  short_description: "Evidence-backed local review with scope control"
  default_prompt: "Use $sam-review-code to review the exact local change with complete diff coverage, verified findings, and a validated decision."
