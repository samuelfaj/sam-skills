interface:
  display_name: "Sam Goal"
  short_description: "Finish a software goal with adaptive execution"
  default_prompt: "Use $sam-goal to complete the requested outcomes. Decompose only when useful, verify the integrated result, and find a new evidence-backed route when a step blocks."
