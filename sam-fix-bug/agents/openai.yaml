interface:
  display_name: "Sam Fix Bug"
  short_description: "Root-cause bugfix with regression proof"
  default_prompt: "Use $sam-fix-bug to reproduce this defect, prove its root cause, apply the smallest safe fix, and validate the evidence."
